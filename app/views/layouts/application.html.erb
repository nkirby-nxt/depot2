<!DOCTYPE html>
<html>
  <head>
    <title>Depot2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <nav class="flex items-center justify-between p-6 container mx-auto">
      <a href=”#”>
   
      </a>
          
      <div class="text-lg text-gray-600 hidden lg:flex">
        <% if user_signed_in? %>
          <%= link_to "Products", products_path, class: "block mt-4 lg:inline-block text-teal-600 lg:mt-0 mr-10"%>
          <%= link_to "Orders", orders_path, class: "block mt-4 lg:inline-block text-teal-600 lg:mt-0 mr-10"%>
        <% end %>
      </div>

          
      <div class="flex items-center">
        <div class="mr-5 lg:mr-0">
          <% if user_signed_in? %>
            <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "py-2 px-6 rounded-md text-gray-600 hover:text-gray-700 text-lg"  %>
         <% else %>
            <%= button_to "Sign In", new_user_session_path, method: :get, class: "py-2 px-6 rounded-md text-gray-600 hover:text-gray-700 text-lg"  %>
        <% end %>
        </div>
        <div class="block lg:hidden">
          <button class="flex items-center px-4 py-3 border rounded text-teal-500 border-teal-500 focus:outline-none">
              <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                  <title>Menu</title>
                  <path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z" />
              </svg>
          </button>
        </div>
      </div>
    </nav>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
