<p style="color: green"><%= notice %></p>

<%= render @order %>

<div>
  <%= link_to "Edit this order", edit_admin_order_path(@order) %> |
  <%= link_to "Back to orders", admin_orders_path %>

  <%= button_to "Destroy this order", admin_order_path(@order), method: :delete %>
</div>
